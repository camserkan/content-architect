# Codex Project Instructions

Read `CODEX_HANDOFF.md` before making changes.

Do not commit `.env` or local secrets.

Use `apply_patch` for manual edits.

Preserve auth, paid access, profile, and credit logic unless the user explicitly asks to change it.
